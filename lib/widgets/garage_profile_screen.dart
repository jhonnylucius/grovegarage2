import 'package:flutter/material.dart';

class GarageProfileScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Garage Profile'),
      ),
      body: Center(
        child: Text('Welcome to Garage Profile Screen'),
      ),
    );
  }
}